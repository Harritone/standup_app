<%= form_with(model: standup) do |form| %>
  <% if standup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(standup.errors.count, "error") %> prohibited this standup from being saved:</h2>

      <ul>
        <% standup.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :standup_date %>
    <%= form.date_select :standup_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
